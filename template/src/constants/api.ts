export const ENDPOINTS = {
  LOGIN: '/posts',
  REFRESH_TOKEN: '/refresh-token', // Set to empty string '' if the app does not use refresh token
  POSTS: '/posts',
};

export const QUERY_KEYS = {
  POSTS: ['posts'],
};
